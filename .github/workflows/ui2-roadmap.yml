name: Verify UI2 roadmap update
on:
  push:
    branches: [feat/ui2-tactical-table-redesign-20260830]
jobs:
  verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply roadmap update
        run: python tools/ui2_roadmap_patch.py
      - name: Run regressions
        run: |
          for f in tests/*.mjs; do node "$f"; done
          git diff --check
      - name: Commit verified roadmap
        run: |
          rm -f tools/ui2_roadmap_patch.py .github/workflows/ui2-roadmap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ROADMAP.md tools/ui2_roadmap_patch.py .github/workflows/ui2-roadmap.yml
          git commit -m "docs: add UI2 tactical table milestone"
          git push
