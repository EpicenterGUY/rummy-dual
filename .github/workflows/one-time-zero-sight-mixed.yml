name: One-time ZERO-SIGHT mixed regression

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-zero-sight-mixed.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply ZERO-SIGHT mixed roadmap/docs patch
        run: python3 .github/scripts/zero_sight_mixed_patch.py
      - name: Install regression
        shell: bash
        run: |
          cp .github/scripts/zero_sight_mixed_regression.mjs tests/zero-sight-mixed-regression.mjs
          sed -i "s|fs.readFileSync('index.html','utf8')|fs.readFileSync(new URL('../index.html',import.meta.url),'utf8')|" tests/zero-sight-mixed-regression.mjs
          sed -i "s|fs.readFileSync('ROADMAP.md','utf8')|fs.readFileSync(new URL('../ROADMAP.md',import.meta.url),'utf8')|" tests/zero-sight-mixed-regression.mjs
          sed -i "s|fs.readFileSync('docs/THEME_GROUPS.md','utf8')|fs.readFileSync(new URL('../docs/THEME_GROUPS.md',import.meta.url),'utf8')|" tests/zero-sight-mixed-regression.mjs
      - name: Run all regressions
        shell: bash
        run: |
          set -euo pipefail
          for f in tests/*.mjs; do
            echo "== $f =="
            node "$f"
          done
      - name: Commit ZERO-SIGHT mixed regression
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ROADMAP.md docs/THEME_GROUPS.md tests/zero-sight-mixed-regression.mjs
          git commit -m 'test: lock ZERO-SIGHT mixed-theme interoperability'
          git pull --rebase origin main
          git push origin HEAD:main
