name: Verify UX1 tutorial flow mobile
on:
  push:
    branches:
      - feat/ux1-tutorial-flow-mobile-20260830
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/ux1-tutorial-flow-mobile-20260830
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply UX1 flow/mobile patch
        run: python tools/ux1_flow_mobile_patch.py
      - name: Run regressions
        run: |
          node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];fs.writeFileSync('/tmp/game.js',m.at(-1)[1]);"
          node --check /tmp/game.js
          for f in tests/*.mjs; do node "$f"; done
          git diff --check
      - name: Commit verified changes
        run: |
          rm -f tools/ux1_flow_mobile_patch.py .github/workflows/ux1-flow-mobile.yml
          git config user.name EpicenterGUY
          git config user.email mintbamtol7@gmail.com
          git add -A
          git commit -m "Stabilize tutorial flow and mobile controls"
          git push origin HEAD:feat/ux1-tutorial-flow-mobile-20260830
